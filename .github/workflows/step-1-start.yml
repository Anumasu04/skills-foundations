name: Step 1 - Start

on:
  push:
    branches:
      - main

jobs:
  step_1:
    runs-on: ubuntu-latest

    permissions:
      issues: write
      contents: read

    steps:
      - name: Create Issue for Step 1
        run: |
          gh issue create \
            --title "Step 1: Welcome to GitHub Skills" \
            --body "🎉 Congratulations! You have successfully triggered your first GitHub Actions workflow. This is Step 1 of the course."
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
